rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if true;
      match /tasks/{taskId} { allow read, write: if true; }
    }
    match /focusSessions/{sessionId} { allow read, write: if true; }
    match /ai_logs/{logId} { allow read, write: if true; }
    // collectionGroup izin (dev) — ai_logs herhangi bir path altında kullanılsa da izin ver
    match /{path=**}/ai_logs/{logId} { allow read, write: if true; }
  }
}
