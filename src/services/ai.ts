export async function generateFocusSuggestion(context: string): Promise<string> {
	// Basit placeholder; gerçek entegrasyon için API çağrısı ekleyin
	return Promise.resolve("Öneri: 25 dakika odaklan, 5 dakika mola ver.");
}
